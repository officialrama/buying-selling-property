# Default values for ms-denwalsus.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nginx
  pullPolicy: IfNotPresent

imagePullSecrets: 
  - name: gcr
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name:

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: NodePort
  port: 3000

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: ms-fe-homespot.service.dce.bri.co.id
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

env: 
  - name: mode
    value: "prd"
  - name: PORT 
    value: "3000"
  - name: SMTP_HOST 
    value: "in-v3.mailjet.com"
  - name: SMTP_PORT 
    value: "465"
  - name: SMTP_AUTH_USER 
    value: "c67b3ec312e33891ef4d873502bac3bc"
  - name: SMTP_AUTH_PASS 
    value: "25ba885cc2e708d6bebebf429df21dec"
  - name: EMAIL_FROM 
    value: "delima@work.bri.co.id"
  - name: MYSQL_DB_HOST	
    value: "10.21.177.3"
  - name: MYSQL_DB_PORT	
    value: "3306"
  - name: MYSQL_DB_USER	
    value: "ivtree" 
  - name: MYSQL_DB_PASS
    value: "P@ssw0rd"	 
