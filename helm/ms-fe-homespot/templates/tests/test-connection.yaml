apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-fe-homespot.fullname" . }}-test-connection"
  labels:
{{ include "ms-fe-homespot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ms-fe-homespot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
